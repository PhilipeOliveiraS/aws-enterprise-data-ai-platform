# TasKiro backend configuration template.
# Copy to .env and set a strong JWT_SECRET, e.g. `openssl rand -hex 48`.
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d
PORT=3000
CORS_ORIGINS=http://localhost:5173
DB_PATH=taskiro.sqlite
