{
  "name": "taskiro-backend",
  "version": "0.1.0",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "JWT_SECRET=test-only-secret-not-a-real-credential DB_PATH=/tmp/taskiro-unit-test.sqlite AI_ENABLED=false bun test"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "3.716.0",
    "@elysiajs/cors": "^1.4.0",
    "@elysiajs/jwt": "^1.4.0",
    "elysia": "^1.4.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  }
}
