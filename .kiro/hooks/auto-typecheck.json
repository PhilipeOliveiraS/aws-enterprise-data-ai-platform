{
  "name": "Automated TypeScript Compiler Check",
  "version": "1.0.0",
  "description": "Executes fast non-emitting typecheck across backend and frontend after tool writes",
  "when": {
    "type": "postToolUse",
    "toolTypes": ["write"]
  },
  "then": {
    "type": "runCommand",
    "command": "cd backend && bun run --bun tsc --noEmit && cd ../frontend && bun run --bun tsc -b --noEmit"
  }
}
